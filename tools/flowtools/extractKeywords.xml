<tool id="extractKeywords" name="Extract Keywords" version="1.0">
  <description>from FCS files.</description>
  <command>
    Rscript --slave --vanilla $GALAXY_ROOT_DIR/tools/flowtools/FCSKeyword.R --args $input $keyword_file
  </command>
  <inputs>
    <param format="fcs" name="input" type="data" label="FCS file"/>
  </inputs>
  <outputs>
    <data format="tabular" name="keyword_file" label="Extract Keywords on ${input.name}"/>
  </outputs>

 <help>
   This tool extracts the Keywords from a FCS file.
   
-----

**Input files**

This tool takes FCS files as input. It can only process one file at a time.

**Output file**

The output is a list of the headers in the provided FCS file.

-----

**Example**

FCSversion=2

$BYTEORD=4,3,2,1

$DATATYPE=I

$NEXTDATA=0

$SYS=Macintosh System Software 9.2.2

CREATOR=CELLQuest 3.3

$TOT=20000

$MODE=L

$PAR=6

$P1N=FSC-H

.

..

...

 </help>
</tool>
